#include <iostream>
#include <stdio.h>
#include <math.h>
using namespace std;

int factorial(int x)
{
    return 0;
}

int main()
{
    float a,b,c;
    printf("Enter a, b, c: ");
    scanf("%f %f %f",&a,&b,&c);
    float y = pow(sin(a),3);
    printf("\ny=%f, x=%f",y,a);

    return 0;
}
